/**
 * {{appName}} Configuration
 *
 * Plugin registration happens between the scaffoldix markers.
 * Patches will insert plugin configurations there.
 */

const plugins = [
  // <scaffoldix:plugins>
  // </scaffoldix:plugins>
];

module.exports = {
  appName: "{{appName}}",
  plugins,
};
